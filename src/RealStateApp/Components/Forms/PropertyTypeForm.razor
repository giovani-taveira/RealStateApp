<MudCard>
    <EditForm Model="@ViewModel" OnValidSubmit="SubmitForm">
        <DataAnnotationsValidator />
        <ValidationSummary />
        <MudCardContent>
            <MudTextField Class="mb-4"
                          Variant="Variant.Filled" @bind-Value="@ViewModel.CurrentPropertyType.Name"
                          Label="Nome"
                          For="@(() => ViewModel.CurrentPropertyType.Name)" />
            <ValidationMessage For="@(() => ViewModel.CurrentPropertyType.Name)" />
        </MudCardContent>
        <MudCardActions Style="padding: 16px; margin-top:-20px;">
            <MudButton Class="ml-auto"
                       ButtonType="ButtonType.Submit"
                       Variant="Variant.Filled"
                       Color="Color.Primary">Salvar</MudButton>
        </MudCardActions>
    </EditForm>
</MudCard>

